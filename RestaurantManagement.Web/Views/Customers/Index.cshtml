@model IReadOnlyCollection<RestaurantManagement.Application.DTOs.Customers.CustomerDto>
@{
    ViewData["Title"] = "Customers";
}

<div class="widget-card">
    <div class="widget-head d-flex align-items-center justify-content-between">
        <div>
            <h5 class="mb-1">Customers</h5>
            <small class="text-muted">@Model.Count customer records</small>
        </div>
        <a class="btn btn-primary" asp-action="Create">Create Customer</a>
    </div>

    <div class="table-responsive mt-3">
        <table class="table shell-table align-middle mb-0">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Phone</th>
                    <th>Email</th>
                    <th>Type</th>
                    <th>Status</th>
                    <th class="text-end">Actions</th>
                </tr>
            </thead>
            <tbody>
            @if (Model.Any())
            {
                foreach (var row in Model)
                {
                    <tr>
                        <td>@row.Name</td>
                        <td>@row.Phone</td>
                        <td>@row.Email</td>
                        <td>@(row.IsWalkIn ? "Walk-in" : "Registered")</td>
                        <td>
                            <span class="badge @(row.IsActive ? "text-bg-success" : "text-bg-secondary")">
                                @(row.IsActive ? "Active" : "Inactive")
                            </span>
                        </td>
                        <td class="text-end">
                            <a class="btn btn-sm btn-outline-secondary" asp-action="Edit" asp-route-id="@row.Id">Edit</a>
                        </td>
                    </tr>
                }
            }
            else
            {
                <tr><td colspan="6" class="empty-state">No customers created yet.</td></tr>
            }
            </tbody>
        </table>
    </div>
</div>
